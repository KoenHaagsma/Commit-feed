<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('partials/head'); %>
    </head>
    <body>
        <%- include('partials/header'); %>
        <main id="leaderboard">
            <section>
                <% for(var i = 0; i < 3; i++) { %>
                <div>
                    <div></div>
                    <p><%= person_commit_count[i].count %> Commits</p>
                    <h2><%= person_commit_count[i].name %></h2>
                    <img src="<%=person_commit_count[i].avatarUrl %>" alt="<%=person_commit_count[i].name %> Image" />
                </div>
                <% } %>
            </section>
            <section>
                <% for(var i = 4; i < person_commit_count.length; i++) { %>
                <article>
                    <div>
                        <h2><%= i %></h2>
                        <img src="<%=person_commit_count[i].avatarUrl %>" alt="" />
                        <p><%= person_commit_count[i].name %></p>
                    </div>
                    <div>
                        <p><%= person_commit_count[i].count %></p>
                    </div>
                </article>
                <% } %>
            </section>
        </main>
    </body>
</html>
